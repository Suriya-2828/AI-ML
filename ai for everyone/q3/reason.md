"The Iris dataset is a small, well-structured benchmark dataset. With the chosen train-test split and a Decision Tree classifier, the classes in the test set were completely separable by the learned decision boundaries, resulting in 100% accuracy. Different random splits may produce slightly lower accuracies."

The Setosa class is perfectly separable from the other two classes using petal measurements.
Versicolor and Virginica overlap a little, but depending on how the train/test split is made, the test set may contain samples that are easy for the model to classify.
